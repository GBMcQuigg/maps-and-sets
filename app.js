// 1. ([1, 2, 3, 4])

// 2. ['ref']

// 3. arr 0 = true / arr 1 = false

const hasDupes = (arr) => new Set(arr) !== arr.length; // ?????????????????????

function vowelCount(str) {
  let vowels = "aeiou";
  for (let i of str) {
    if (str.includes(vowels)) {
      // ??????????????
    }
  }
}
